name: Proceso ICS SNE (Blob -> PostgreSQL)

on:
  schedule:
    # 03:00 AM Bogotá (UTC-5) = 08:00 UTC
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  procesa_ics_sne:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read

    steps:
      - name: 📦 Checkout repositorio
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: ⬆️ Actualizar pip
        run: python -m pip install --upgrade pip

      - name: 📦 Instalar dependencias
        run: |
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt --no-cache-dir
          else
            echo "❌ No existe requirements.txt"
            exit 1
          fi

      # 🔎 Verifica ruta real (según tu repo: GestionExpress/jobs/)
      - name: 🔎 Verificar script GestionExpress/jobs/procesa_ics_sne.py
        run: |
          if [ ! -f GestionExpress/jobs/procesa_ics_sne.py ]; then
            echo "❌ No se encontró GestionExpress/jobs/procesa_ics_sne.py"
            echo "📁 Contenido del repo:"
            ls -la
            echo "📁 Contenido de GestionExpress/:"
            ls -la GestionExpress || true
            exit 1
          fi

      - name: 🚀 Ejecutar proceso ICS SNE
        working-directory: GestionExpress
        env:
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          DATABASE_PATH: ${{ secrets.DATABASE_PATH }}
          DB_POOL_MIN: ${{ secrets.DB_POOL_MIN }}
          DB_POOL_MAX: ${{ secrets.DB_POOL_MAX }}
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔄 Iniciando proceso ICS SNE"
          python --version
          echo "📁 Working dir: $(pwd)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          python -m jobs.procesa_ics_sne

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Proceso finalizado correctamente"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: ❌ Mostrar link del error
        if: failure()
        run: |
          echo "El proceso falló."
          echo "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"